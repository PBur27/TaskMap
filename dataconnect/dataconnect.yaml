specVersion: "v1"
serviceId: "psm"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "psm-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
